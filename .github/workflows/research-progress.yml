name: Research progress consistency

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: scripts/requirements-progress.txt
      - run: python -m pip install -r scripts/requirements-progress.txt
      - run: python -m unittest discover -s tests -v
      - run: python scripts/render_research_progress.py --check
